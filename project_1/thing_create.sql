CREATE TABLE `assignment_1`.`thing` (
  `title` VARCHAR(45) NOT NULL,
  `developer` VARCHAR(45) NULL,
  `genre` VARCHAR(45) NULL,
  `gameMode` VARCHAR(45) NULL,
  `platform` VARCHAR(45) NULL,
  `price` DECIMAL(10,2) NULL,
  PRIMARY KEY (`title`));